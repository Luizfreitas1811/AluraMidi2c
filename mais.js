function toca som(selectorAudio)
consert elemento= document.query Seletor
(seletor Audio)
if(elements 88 elemento.localName==='audio')
     elemento.play( ).
}
else{
   console.10g("elemento não encontrado ou seletor invalido")
  }
}
cost lista deteles-document queery 
for(let contador=0; contador <histos deleteclas.leugt; contador++)
